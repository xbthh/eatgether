<table table="table-boldered table-hover">
    <thead>
        <tr>
            <th>#</th>
            <th>约饭主题</th>
            <th>约饭内容</th>
        </tr>
    </thead>

    <tbody>
        <% @posts.each do |post| %>
        <th>
            <td><%= post.title %></td>
            <td><%= post.description %></td>
        </th>
        <% end %>
    </tbody>
</table>
